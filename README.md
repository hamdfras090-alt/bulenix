# 🛍️ متجر Bluenix الإلكتروني

<div dir="rtl">

متجر إلكتروني حديث وشامل مبني بـ HTML, CSS, JavaScript ومربوط بقاعدة بيانات Firebase السحابية.

## ✨ المميزات الرئيسية

### 🏪 واجهة المتجر (للعملاء)
- 🎨 تصميم عصري وجذاب مع دعم المظهر الليلي/النهاري
- 🔍 بحث فوري أثناء الكتابة
- 🖼️ سلايدر صور احترافي في الصفحة الرئيسية
- 📱 تصميم متجاوب (Responsive) لجميع الأجهزة
- ⭐ نظام تقييم المنتجات بالنجوم
- 🛒 سلة تسوق متقدمة مع حساب تلقائي
- 💳 دعم طرق دفع متعددة (COD + حوالة بنكية)
- 🎁 نظام كوبونات الخصم
- 📦 صفحة تفاصيل منتج احترافية
- 🚚 نظام تتبع الطلبات في الوقت الفعلي

### 🎛️ لوحة التحكم (للمديرين)
- ➕ إضافة/حذف/تعديل المنتجات
- 🖼️ إدارة صور السلايدر
- 📊 عرض وإدارة الطلبات
- 🔄 تحديث حالة الطلبات
- 📈 إحصائيات فورية

### ☁️ التكامل مع Firebase
- ⚡ تحديثات فورية (Real-time)
- 💾 تخزين سحابي آمن
- 🌐 الوصول من أي مكان
- 📱 قابلية التوسع

## 📁 بنية المشروع

</div>

```
متجر-bluenix/
├── index.html              # واجهة المتجر الرئيسية
├── main.js                 # منطق المتجر
├── style.css               # تنسيقات المتجر
│
├── sss.html                # لوحة التحكم
├── sss.js                  # منطق لوحة التحكم
├── sss.css                 # تنسيقات لوحة التحكم
│
├── firebase-config.js      # إعدادات Firebase
│
├── logo.jpg                # شعار المتجر
├── *.png, *.jpg            # صور وبانرات
│
└── docs/                   # التوثيق
    ├── FIREBASE_SETUP.md   # دليل إعداد Firebase السريع
    ├── Firebase_Integration_Guide.md  # دليل Firebase الشامل
    └── ملاحظات_النظام.txt  # ملاحظات ودليل المستخدم
```

<div dir="rtl">

## 🚀 البدء السريع

### المتطلبات
- متصفح ويب حديث (Chrome, Firefox, Safari, Edge)
- اتصال بالإنترنت
- حساب Firebase (مجاني)

### خطوات التشغيل

#### 1️⃣ إعداد Firebase
راجع ملف [`FIREBASE_SETUP.md`](./FIREBASE_SETUP.md) للحصول على دليل خطوة بخطوة.

باختصار:
1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. افتح مشروع **bluenix-eff02**
3. اذهب إلى **Firestore Database** → **Rules**
4. الصق القواعد البسيطة وانشرها

#### 2️⃣ تشغيل المتجر
```bash
# ببساطة افتح الملف في المتصفح
index.html
```

أو باستخدام خادم محلي:
```bash
# Python 3
python -m http.server 8000

# Node.js (مع npx)
npx serve
```

ثم افتح: `http://localhost:8000`

#### 3️⃣ تشغيل لوحة التحكم
```bash
# افتح في المتصفح
sss.html
```

## 📚 التوثيق

### ملفات الدليل
- **[FIREBASE_SETUP.md](./FIREBASE_SETUP.md)** - إعداد Firebase في 5 دقائق
- **[Firebase_Integration_Guide.md](./Firebase_Integration_Guide.md)** - دليل التكامل الشامل
- **[ملاحظات_النظام.txt](./ملاحظات_النظام.txt)** - معلومات وتعليمات عامة

### استخدام المتجر

#### إضافة منتج جديد
1. افتح `sss.html`
2. اختر تبويب "إدارة المنتجات"
3. املأ البيانات:
   - اسم المنتج
   - السعر
   - التصنيف
   - الوصف
   - الحالة (متوفر/طلب مسبق)
   - صورة المنتج
4. اضغط "نشر المنتج"
5. ✅ سيظهر المنتج فوراً في المتجر!

#### إدارة الطلبات
1. افتح `sss.html`
2. اختر تبويب "الطلبات الواردة"
3. ستجد جميع الطلبات مع معلومات كاملة
4. يمكنك:
   - تحديث حالة الطلب
   - حذف الطلب
   - عرض تفاصيل العميل

#### إضافة صور للسلايدر
1. افتح `sss.html`
2. اختر تبويب "إدارة السلايدر"
3. ارفع الصورة
4. اضغط "إضافة للسلايدر"
5. ✅ ستظهر الصورة في الصفحة الرئيسية!

## 🛢️ بنية قاعدة البيانات

### Collections في Firestore

#### `products`
```javascript
{
  id: Number,           // معرف فريد
  title: String,        // اسم المنتج
  price: String,        // السعر
  category: String,     // التصنيف
  desc: String,         // الوصف
  status: String,       // "active" أو "preorder"
  img: String,          // صورة المنتج (Base64)
  createdAt: String,    // تاريخ الإضافة
  ratingStats: {        // إحصائيات التقييم
    sum: Number,
    count: Number
  }
}
```

#### `sliderImages`
```javascript
{
  id: Number,           // معرف فريد
  img: String,          // صورة السلايدر (Base64)
  createdAt: String     // تاريخ الإضافة
}
```

#### `orders`
```javascript
{
  id: Number,               // رقم الطلب
  timestamp: Number,        // الوقت (timestamp)
  customerName: String,     // اسم العميل
  customerPhone: String,    // رقم الهاتف
  customerCity: String,     // المدينة
  customerArea: String,     // المنطقة
  customerAddress: String,  // العنوان التفصيلي
  items: Array,             // المنتجات المطلوبة
  total: String,            // الإجمالي
  status: String,           // حالة الطلب
  paymentMethod: String     // طريقة الدفع
}
```

## 🎨 التخصيص

### تغيير الألوان
في `style.css` و `sss.css`، يمكنك تعديل المتغيرات:

```css
:root {
  --primary-color: #3498db;
  --secondary-color: #2ecc71;
  --danger-color: #e74c3c;
  /* ... والمزيد */
}
```

### تغيير الشعار
استبدل ملف `logo.jpg` بشعارك الخاص.

### إضافة تصنيفات جديدة
في `sss.html`، عدّل قائمة التصنيفات:
```html
<select id="pCategory" required>
  <option value="إلكترونيات">إلكترونيات</option>
  <option value="ملابس">ملابس</option>
  <!-- أضف تصنيفاتك هنا -->
</select>
```

## 🔐 الأمان

### ⚠️ ملاحظة مهمة
القواعد الحالية في Firestore تسمح بالقراءة والكتابة للجميع. هذا **للتجربة فقط**.

### للإنتاج الحقيقي:

1. **أضف Firebase Authentication:**
```javascript
import { getAuth, signInWithEmailAndPassword } from 'firebase/auth';
```

2. **حدّث قواعد Firestore:**
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // السماح بالقراءة للجميع
    match /{document=**} {
      allow read: true;
    }
    
    // الكتابة فقط للمستخدمين المصادق عليهم
    match /products/{productId} {
      allow write: if request.auth != null;
    }
    
    match /orders/{orderId} {
      allow create: true;
      allow update, delete: if request.auth != null;
    }
  }
}
```

## 🚀 التحسينات المستقبلية

- [ ] إضافة صور متعددة لكل منتج
- [ ] نظام مراجعات وتعليقات
- [ ] استخدام Firebase Storage للصور
- [ ] إضافة لوحة تحليلات (Analytics)
- [ ] دعم تعدد اللغات
- [ ] نظام إشعارات فورية
- [ ] بوابة دفع إلكترونية
- [ ] تطبيق موبايل

## 🤝 المساهمة

هذا مشروع خاص، لكن يمكنك:
- الإبلاغ عن المشاكل
- اقتراح ميزات جديدة
- تحسين التوثيق

## 📞 الدعم

للحصول على المساعدة:
1. راجع ملفات التوثيق في مجلد `docs/`
2. تحقق من Developer Console (F12) لرؤية الأخطاء
3. راجع [Firebase Documentation](https://firebase.google.com/docs)

## 📜 الترخيص

هذا المشروع ملك لشركة Bluenix.

## 💙 شكر خاص

- Firebase لقاعدة البيانات السحابية
- Lucide Icons للأيقونات الجميلة
- Google Fonts للخطوط الاحترافية

---

**تم بناؤه بـ 💙 لشركة Bluenix**

*آخر تحديث: فبراير 2026*

</div>
